version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - echo "==== DIRECTORY LISTING START ===="
            - ls -R
            - echo "==== DIRECTORY LISTING END ===="
      artifacts:
        baseDirectory: dist
        files:
          - "**/*"
    cache:
      paths:
        - node_modules/**/*
