version: 1
frontend:
  phases:
    preBuild:
      commands:
        - ls -R # This prints all files so you can see the real structure in the logs
    build:
      commands:
        - echo "Debugging structure"
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
