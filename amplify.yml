version: 1
applications:
  - appRoot: .
    backend:
      phases:
        build:
          commands:
            - cd client && npm ci
            - cd client && npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
    frontend:
      phases:
        build:
          commands:
            - npm ci
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - "**/*"
      cache:
        paths:
          - client/node_modules/**/*
